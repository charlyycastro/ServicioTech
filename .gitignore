# --- SEGURIDAD (Claves y Secretos) ---
.env
*.env
.DS_Store

# --- ARCHIVOS DE USUARIO (Firmas, Fotos, PDFs generados) ---
# Ignoramos todo lo que esté dentro de media
media/
# Pero (opcional) mantenemos la carpeta vacía para que no de error
!media/.gitkeep 

# --- BASE DE DATOS LOCAL ---
db.sqlite3

# --- PYTHON Y ENTORNOS ---
__pycache__/
*.pyc
.venv/
venv/
env/

# --- ARCHIVOS ESTÁTICOS RECOPILADOS (Si usas collectstatic) ---
staticfiles/